<Page x:Class="ClientManager.App.Views.ClientListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Clients" Loaded="ClientListPage_OnLoaded">
    <Grid Background="#ECF0F1" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Client List" FontSize="22" FontWeight="Bold" 
                       Foreground="#2C3E50" DockPanel.Dock="Left" VerticalAlignment="Center" />
            <Button Content="+ New Client" Style="{StaticResource PrimaryButton}" 
                    Click="BtnNewClient_Click" DockPanel.Dock="Right" HorizontalAlignment="Right" />
        </DockPanel>

        <!-- Search bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="4" Padding="10" 
                Margin="0,0,0,10" BorderBrush="#DDD" BorderThickness="1">
            <DockPanel>
                <Button Content="Search" Click="BtnSearch_Click" DockPanel.Dock="Right" 
                        Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" />
                <Button Content="Clear" Click="BtnClear_Click" DockPanel.Dock="Right" 
                        Background="#95A5A6" Foreground="White" Padding="15,8" FontSize="13"
                        Cursor="Hand" BorderThickness="0" Margin="5,0,0,0" />
                <TextBox x:Name="txtSearch" Style="{StaticResource FormTextBox}" 
                         Margin="0" VerticalAlignment="Center" />
            </DockPanel>
        </Border>

        <!-- Client grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="4" Padding="10" 
                BorderBrush="#DDD" BorderThickness="1">
            <DataGrid x:Name="dgClients" MouseDoubleClick="DgClients_MouseDoubleClick"
                      SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ClientId}" Width="50" />
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200" />
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="130" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90" />
                    <DataGridTextColumn Header="Risk" Binding="{Binding RiskProfile}" Width="80" />
                    <DataGridTextColumn Header="Onboarded" Binding="{Binding DateOnboarded, StringFormat=dd/MM/yyyy}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
